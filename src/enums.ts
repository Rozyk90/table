enum columns {
  name = "name",
  username = "username",
  email = "email",
  phone = "phone",
}

enum status {
  idle='idle',
  loading='loading',
  succeeded='succeeded',
  failed='failed'
}

export { columns,status };
